# Build Log

Chronological record of how Randomware is being built, kept for the OpenAI Build Week submission. Newest entries last.

## 2026-07-18 — Preparation (before the Codex build)

- Product direction consolidated into [PRD.md](PRD.md). No technical design decisions were made; everything architectural is explicitly reserved for the design pass (PRD §14).
- 29 candidate public APIs verified with live requests ([api-candidates/report.md](api-candidates/report.md)). 15 primary + 9 backup adopted; 4 rejected (no HTTPS, TLS failure, persistent 504, wrong content type). Real response bodies and headers captured under `api-candidates/samples/` as fixture material.
- Repository created: public, MIT licensed.
- Tooling disclosure: this preparation step (PRD authoring and API verification) was done with Claude Code, before and outside the $100 Codex budget. No application code was written during preparation.
- Design-pass brief added ([DESIGN_BRIEF.md](DESIGN_BRIEF.md)): the instruction document handed to the GPT-5.6 Sol design session, so the design pass starts from a single "read this and execute" prompt.

## 2026-07-18 — Product shape decision (pre-design)

- Owner decision: generation must be billed to the executing user's own ChatGPT plan, not the owner's API key. Verified against current platform capabilities: an external-web "sign in with ChatGPT and bill the user's plan" API is not shipped (feature-request stage); the Apps SDK is the shipped mechanism.
- Product shape fixed as **hybrid**: a ChatGPT app (Apps SDK — spin + generation, refereed by MCP tools) plus a companion showcase site (creation URLs, sandboxed execution, gallery). PRD and design brief updated accordingly.
- Four undocumented Apps SDK risks moved into a mandatory first-milestone spike: tool-argument size for HTML artifacts, widget CSP allowlist reliability, audio playback, `frameDomains` embedding of the companion site.

## 2026-07-18 — Adversarial feasibility review (pre-design)

- An independent adversarial review (a fresh Claude subagent with no shared conversation context, verifying platform claims against official docs) audited the PRD and design brief for design-pass readiness. Verdict: NOT READY — 1 P0, 7 P1, 8 P2, 5 P3 findings. Every finding was individually verified by the orchestrator and all were accepted; none were vacuous.
- Headline corrections applied: containment enforcement restated as the owner's proxy + per-surface CSP (a widget CSP cannot govern creation pages — the P0); the DOM-injection fallback prohibited (it would expose `window.openai` to generated code); creation retention defined (through the judging window, no commitment beyond); choreography non-compliance made a first-class failure with a widget-owned timeout; model-pinning claims removed (choreography must be model-agnostic); a UGC publication policy added (password/payment-input rejection, owner-controlled chrome with report link, curated gallery, instant unpublish); an audio/media exception defined (mediation-resolved stream URLs only); registry policy extended to secondary asset domains and provider demo keys; latency/quality metrics scoped to the owner's demo environment; per-upstream API budgets added; judge instructions now state developer-mode plan prerequisites; pre-authorized descopes added to §15 (link-out as the Must bar, raw traffic list, minimal showcase index); demo outline re-timed to ≤2:50.
- Correction to earlier entries: "29 candidate APIs, 15 primary + 9 backup" reflects round 1 only; after round 2 the totals are 35 APIs across 36 checks, 17 primary + 14 backup, 4 rejected.

## 2026-07-18 — Design phase closed; budget checkpoints recorded

- Sol design phase complete: five design documents (`afa3995`) plus three bounded addenda — incremental reading requirements (`b655361`), 18-entry launch registry (`3a7f05e`), and independent review checkpoints (`e347087`).
- Meters at design end, owner-transcribed from Codex Settings > Usage at 2026-07-18 11:21 JST: plan usage 33% of the weekly limit (ChatGPT Plus), grant credit balance 2,500 — untouched (credit delta 0). Codex consumes plan-included usage before grant credits, so the design phase cost plan quota only. The baseline plan percentage before the design session was not captured; recorded as an honest omission.
- Auto top-up is off and Codex is signed in with ChatGPT (owner-confirmed). The Luna `/goal` implementation run starts from this snapshot in the same primary session.

### Planned next steps

1. GPT-5.6 Sol (high reasoning effort): technical design pass producing `docs/ARCHITECTURE.md`, `docs/PLAN.md`, `docs/ACCEPTANCE.md`, `docs/BUDGET.md`, `docs/GOAL.md`. Documents only — no code.
2. Switch the session model to GPT-5.6 Luna (max reasoning effort) and run the single Codex `/goal` implementation session driven by `GOAL.md`.
3. Record credit usage at each checkpoint in `BUDGET.md`; after core functionality is complete, run `/feedback` in the implementation session and record the Session ID here.

## 2026-07-18 — Implementation foundation (current primary session)

- The repository now has a dependency-free Node vertical slice behind the documented boundaries: deterministic selector, 18-entry fixed-operation registry, concept/artifact validators, in-memory run state machine, HMAC capabilities, mediated fixture/live broker, owner creation/failure pages, sandbox harness, public companion UI, and an eight-tool JSON-RPC MCP surface.
- Test-first evidence: `npm ci --ignore-scripts --no-audit --no-fund` completed; 19 unit tests, 3 loopback integration tests, and 1 containment test passed. The sandbox disallows local TCP listeners, so HTTP suites were rerun with the approved ephemeral-loopback elevation. `npm run acceptance:machine` passed all offline checks (format, parse/lint, typecheck, unit, integration, e2e, build, registry fixture verification, public-surface security scan, secret scan, and required-document checks).
- The selector's 5,000-seed distribution assertion measured a 14–16% three-way band and an inclusion max/min ratio below 2.5 for the representative 18-entry registry. The 18 fixture entries are verified locally; `registry:verify:live` remains an explicitly offline-safe report until a networked deployment check is available.
- This is not yet Milestone 0 completion: real ChatGPT developer-mode transport, Apps SDK artifact-size/CSP/audio/frame evidence, Cloudflare Free CPU/D1 evidence, public deployment, and owner-transcribed implementation budget meters remain external gates. No live/deployed or real-ChatGPT claim is made here.
- Local browser evidence (Codex in-app browser, 2026-07-18 JST): the companion loaded, Spin revealed two entries, Build produced a creation URL, the owner warning/source/request chrome remained outside the iframe, and the generated frame exposed only its bounded interaction. Navigation to `chatgpt.com` was refused by the browser safety policy, so no real ChatGPT session or Apps SDK claim was attempted; the local server was stopped after the check.
- Gate 0 stop: `BUDGET.md` §4.1 requires a fresh Codex Settings snapshot of both plan usage and grant balance before a milestone can start; those meters are not available in this app context. The real ChatGPT developer-mode artifact-transport/CSP/audio/frame checks and the Cloudflare Free 40 KiB CPU/D1 spike are also unperformed. Per the contract, the working tree is preserved and the owner must provide the usage snapshot plus access to the real ChatGPT/Cloudflare feasibility environment before Milestone 0 can be closed or Milestone 1 can begin.
- Local feasibility evidence: `npm run feasibility:local` produced byte-perfect, validator-accepted 10,000-, 25,000-, and 40,000-byte artifacts with matching SHA-256 hashes. This verifies only the local transport/validator implementation; it does not substitute for Apps SDK tool-argument delivery or the Workers Free/D1 repeated-run check.
- Continued local preparation: the companion now records concept rerolls and recent specimens, exposes human-readable dataflow/unused status, supports report and authenticated unpublish removal, and exercises real signed-capability fixture calls in the containment suite. Eighteen adapted-output goldens are generated beside the preserved raw fixtures; `registry:verify` validates both sets. A Cloudflare-shaped `fetch` handler, `wrangler.jsonc`, and initial D1 migration are present and tested without a listener. These are implementation preparations, not a deployment claim: the D1 ID is intentionally owner-supplied and Wrangler/Cloudflare credentials are not present in this environment.
- Deployment probe: Wrangler 4.112.0 is available through `npx`; `wrangler whoami` reports no authenticated account. The checked-in migration applies successfully to Wrangler's local D1 emulator (`npx wrangler d1 migrations apply randomware --local`, 6 commands). Remote D1 creation/deploy remains an owner-authorized external action.
- Worker bundle evidence: `npx wrangler deploy --dry-run` succeeds with 51.29 KiB total asset upload, `env.DB` D1, `env.ASSETS`, the fixture flag, and the hourly cron binding. This is a no-deploy check only; no public Worker or remote D1 was created.
- Local Worker/D1 evidence: Wrangler local runtime served `/healthz` and `/api/spin`; after stopping and restarting the Worker, `GET /api/runs/run_33fe10492b69e880b9136596b6028491` returned the original run from D1. This proves local durable storage across a process boundary, not remote production availability.
- Local D1 creation evidence: through Wrangler 4.112.0, a second spin → full concept contract → validated 10–40 KiB artifact → `/c/:id` flow returned 200 at every boundary and rendered the persisted concept. The local Worker was stopped afterward; no remote resource was touched.
- The Worker now selects fixture/live broker mode from `RANDOMWARE_FIXTURES` and takes the signing secret from the deployment secret binding; the no-deploy bundle remains green.
- The Worker-shaped MCP handler now implements all eight listed tool calls (concept, artifact, repair, run recovery, mutation, and choreography failure included), with the full concept contract required in its input schema; the local fetch test covers spin and run recovery.
- The deployed smoke command (`npm run test:e2e:deployed -- --base-url=...`) is now prepared for the owner-provided HTTPS origin. It intentionally refuses localhost and does not run until a public deployment exists.
- Fresh checkpoint verification at commit `b0f0adf`: clean `npm ci`; `npm run acceptance:machine` passed (29 unit, 4 integration, 1 containment/e2e plus build, registry, security, secrets, and document checks); `npm run feasibility:local` passed all three byte-perfect sizes; `npx wrangler deploy --dry-run` passed; `git diff --check` and clean-tree checks passed.
- Health evidence: deterministic transition tests cover degraded/disabled/restore semantics; the scheduled Worker runs one fixed representative operation per registry entry, publishes `api_health` to D1, and D1-backed spins exclude non-healthy rows when present. No remote cron has run yet.
- Bounded live registry check (2026-07-18 12:23 JST, Wrangler/network environment): 14/18 entries returned valid JSON within their caps. Healthy: Deck of Cards, PoetryDB, Datamuse, Art Institute, Radio Browser, Open-Meteo, Frankfurter, RandomUser, USGS, Nager.Date, TVMaze, Rick and Morty, LibriVox, and TheMealDB. Disabled/unavailable for this report: Dog CEO (timeout), Wikipedia On This Day (HTTP 404 for the fixed 2024 path), Met Museum (HTTP 404 for object 1), and Open Food Facts (network error). `registry:health:publish` recorded the 14 healthy IDs for explicit D1 publication; the launch remains above the 10-entry floor. Full report is local runtime evidence and no raw fixtures were changed.
- Second local browser evidence (Codex in-app browser, 2026-07-18 JST): sequential reels revealed a two-API spin, a concept reroll recorded no artifact, Build produced a creation and recent-index card, and Spin again selected a history-distinct three-API set whose third reel appeared after the reveal delay. The browser tab and local server were closed after verification.
- Submission-document evidence: `document-check` now verifies the required README sections, MIT license, ≤200-character pitch, demo-script attribution, project description, and all design/acceptance documents. It passes locally; the deployed URL, public repository verification, video, feedback Session ID, and live ChatGPT evidence remain intentionally unfilled.
- Fresh local checkpoint (2026-07-18 JST, commit after `13a8de2`): with the approved loopback elevation, `npm run acceptance:machine` passed all offline checks (29 unit, 4 integration, 1 containment/e2e, build, registry, security, secrets, and document checks); `npm run feasibility:local` passed byte-perfect 10,000/25,000/40,000-byte validation; and `npx wrangler deploy --dry-run` passed with the D1, assets, and fixture bindings. These results do not close Gate 0 or substitute for real ChatGPT transport, Cloudflare Free CPU/D1, authenticated deployment, or deployed-browser evidence.
- Owner-transcribed meter snapshot (2026-07-18 12:31 JST): the ChatGPT Plus weekly plan meter reset after the `/goal` start snapshot and now shows **99% of the weekly limit remaining**; the grant credit balance is **2,500**, unchanged since redemption (credit delta 0). This satisfies unblock item 1 of the Gate 0 hard stop. The remaining unblock items — authenticated Wrangler/D1/signing secret, real ChatGPT developer-mode Apps SDK evidence, and public deployment — are owner actions in progress. Continuity rule recorded in `BUDGET.md` §8.
- Public deployment evidence (2026-07-18 12:52 JST, source commit `c4278b6`): `npx wrangler whoami` confirmed the owner OAuth account; D1 `randomware` was created in APAC (`ece51c24-5807-45d2-8c5f-9c2d080cf5de`) and migrations `0001_initial.sql` and `0002_run_metadata.sql` applied remotely. A fresh signing secret and a separate owner bearer secret were submitted through `wrangler secret put`; neither value was written to disk, output, or source. The code deployment was version `cc80e08f-f3f9-4681-86ab-139cffd67437`; the later owner-secret update created the currently active version `deb3a42c-b329-4425-9c88-8aa596d7a313`. The Worker is at `https://randomware.randomware.workers.dev/` with `/mcp` at `https://randomware.randomware.workers.dev/mcp`. After workers.dev certificate propagation, `npm run test:e2e:deployed` passed (`healthz`, public index, and all 8 MCP tools); read-only health, registry, recent-index, and MCP checks also returned 200. A broader production create/report/unpublish mutation was not run after the safety guard rejected that unrequested state change. Real ChatGPT Apps SDK feasibility evidence and the full owner acceptance protocol remain pending.

- Production Gate 5 attempt (2026-07-18 13:05 JST): the first deployed 40,000-byte/20-run script stopped at run 0 because `/api/spin` returned `400 {"code":"no_healthy_combination"}`; no artifact was accepted and no 40 KiB D1 write occurred. Remote D1 inspection showed all 18 `api_health` rows marked `degraded` with `reason=upstream_failure` by the 04:00 UTC cron check. Three representative upstreams returned 200 JSON from the verification machine, so this was not recorded as an upstream outage. Workers Tail for the failed request reported a normal response (`cpuTime: 5`, `wallTime: 484`) and no exception.
- Root cause and redeploy (2026-07-18 13:08 JST): a regression test first failed when a platform-style fetch required `this === globalThis`; `Broker` had stored `globalThis.fetch` and called it with the broker instance as receiver. `Broker` now binds its fetcher to `globalThis` (`tests/unit/broker.test.js` covers the regression); the targeted broker suite is green. The fix was deployed as Worker version `719a3932-a3c7-47ad-a8f1-05063c0ec0d6`. The next scheduled check must revalidate the existing degraded rows; the requested production Gate 5 pass remains open until `/api/spin` succeeds and all 20 exact 40,000-byte artifact/D1 runs complete. A manual all-row health override was not performed because it would bypass the deployed health gate.
- Widget/frame decision: the deployed `/mcp` is a JSON-RPC tool surface only; it declares no widget resource metadata and no `frameDomains`. Record the frame test as **not declared — embedding stays off; link-out is final**.
- MCP lifecycle and widget-resource implementation checkpoint (2026-07-18 13:28 JST): the Worker now negotiates MCP `2025-06-18` (with the older supported versions), returns `capabilities`, `serverInfo`, and instructions from `initialize`, accepts `notifications/initialized` with HTTP 202 and an empty body, answers `ping`, serves HTTP 405 for unsupported GET `/mcp`, and exposes `resources/list` plus `resources/read`. The `open_randomware` tool references `ui://widget/randomware.html`; the resource uses `text/html;profile=mcp-app`, Apps SDK `_meta.ui` CSP metadata for the companion origin, and `openai/widgetCSP.redirect_domains`, with `frameDomains` deliberately absent. The widget renders sequential reveal, calls `spin_apis` through the ChatGPT bridge, persists widget state, and presents link-out as the final embedding fallback. Local worker/server lifecycle tests and `npm run acceptance:machine` pass; deployment and the initialize-first production probe follow this checkpoint.
- Production registry publication and Gate 5 verification (2026-07-18 13:28 JST): `npm run registry:verify:live` checked all 18 entries and found 16 healthy (Rick and Morty, Open Food Facts, LibriVox, and TheMealDB included); Wikipedia On This Day and Met Museum remain disabled for fixed-probe HTTP 404s. `registry:health:publish` wrote all 18 statuses to remote D1 (`16 healthy`, `2 disabled`, source check `2026-07-18T04:18:11.632Z`). The first Gate 5 harness run accepted a 40,000-byte artifact but exposed a harness assertion-path bug (`bytes` is under `revisions[]`), so it was not counted. A clean follow-up completed 20/20 deployed runs with exact 40,000-byte accepted revisions and D1 writes: total latency 303–503 ms (average 332 ms), spin 58–214 ms (average 73 ms), concept 106–188 ms (average 117 ms), artifact 132–171 ms (average 142 ms). Wrangler Tail samples showed `outcome:ok`, CPU 2–21 ms and wall time 51–123 ms, with no CPU-limit errors.
- Production lifecycle redeploy (2026-07-18 13:30 JST, commit `f0796a4`): Worker version `f22caff0-a8c6-49e0-b572-c48a7d6809d2` deployed to `https://randomware.randomware.workers.dev`. After edge propagation, the initialize-first external probe and `npm run test:e2e:deployed` passed: `initialize` 200 with negotiated `2025-06-18`, `notifications/initialized` 202 empty, `ping` 200, `resources/list`/`resources/read` 200 with `text/html;profile=mcp-app` and Apps SDK metadata, `tools/list` 200 with 8 tools and `open_randomware` template metadata, and GET `/mcp` 405 with `Allow: POST`. The resource has no `frameDomains`; the chosen result remains **not declared — embedding stays off; link-out is final**.
- MCP CallToolResult contract fix (2026-07-18, commit `7b48b68`): every tool result now contains a concise text `content` block alongside its existing `structuredContent`; error results retain `isError` and Apps SDK tool metadata remains unchanged. The deployed e2e now invokes all eight tools, including artifact rejection/repair and choreography failure, and checks 10 CallToolResult responses for the full shape. Worker version `3b69f5fd-e11a-4308-9571-db275b04581b` passed the production run: 8 tool names covered, 10 result-shape checks, registry 18, protocol `2025-06-18`, widget MIME `text/html;profile=mcp-app`.
- Deployed e2e idempotency hardening (2026-07-18): the harness now uses a unique per-execution request-ID namespace so D1 idempotency cannot reuse a prior run. The fresh production run passed all 8 tool names and 10 full CallToolResult shape checks against Worker version `3b69f5fd-e11a-4308-9571-db275b04581b`.
- Real ChatGPT Apps SDK evidence (owner update, 2026-07-18): the baseline `open_randomware` call passed and the widget rendered. Developer mode displayed a **CSP off** badge, so the spike records that declared widget `connectDomains` are not enforced on dev-mode apps; no production design change follows because containment remains the mediated broker plus companion page CSP. The `apps-sdk-10000` run is intentionally parked at `concept_accepted` after composition interruption and is left unchanged.
- MCP annotation correction (2026-07-18, commit `e10338e`): production `tools/list` now reports `openWorldHint:false` for `open_randomware`, `spin_apis`, `get_run`, `mutate_creation`, and `record_choreography_failure`, while `submit_concept`, `submit_artifact`, and `submit_repair` remain open-world. Worker version `57221bd2-b4ff-46d7-af0a-eee75fc61b42` passed the deployed e2e (8 tool names, 10 CallToolResult checks).

## 2026-07-18 — Apps SDK spike evidence closed and owner retuning

- Transport evidence is **closed by owner decision**; no additional size run is required. In real ChatGPT developer mode with GPT-5.6, run `run_4d116a0f6379a24b40915afdb8367311` failed its first artifact revision, then accepted one live repair at 10,179 bytes. The accepted SHA-256 is `1fea412431fc97d3237e03843234e9bff5c9879d3690dd94f6bad8c7ddca577d`, matching production D1; the public creation is **Bark Exchange**.
- The 40 KiB-region run `run_9e203159afbcaf6b5ee8007b2e92e4f5` delivered a 40,357-byte artifact intact to the validator, which correctly returned `artifact bytes 40357 outside 10000..40000`. Its one repair was a 483-byte fragment and became terminal failed specimen `creation_d809a72a3e0bd34546adb7cf79b8820b`. A later out-of-phase submit was rejected with `phase_or_idempotency`, confirming referee integrity. The arrival proves tool-argument capacity at or above the product cap; the policy rejection is the expected result.
- `run_dc4a591b81f3fa9bb9ff8a8b3f12ce17` stalled at `concept_accepted` because the model could not finish a 34–38 KB single-message composition. This is model-output endurance, not a transport ceiling. The 25 KB point was skipped by owner decision because the 40,357-byte arrival subsumed it. Conclusion: no platform tool-argument ceiling below the 40,000-byte product cap was observed; byte-intact acceptance is proven through 10,179 bytes after full validation. ChatGPT's safety layer intermittently blocks parameterized tool calls (OpenAI staff acknowledged the platform issue on 2026-07-10); parameterless natural-usage calls pass reliably, while user-dictated instruction-like parameter values are highest risk. Record this as an acceptance-protocol caveat.
- **Bark Exchange** — a dog CEO plus frankfurter becomes a deadpan currency kiosk where a random dog interprets USD-to-JPY as a travel omen — is the first live owner confirmation of the intended collision/deadpan thesis. The owner judged the absurdity exactly right.
- Contract correction from the live timings: the widget and `ARCHITECTURE.md §3.3` now use a 60 s/120 s concept first/terminal deadline and 300 s/900 s artifact and repair first/terminal deadlines. The widget re-steers once, then records stable `choreography_timeout`; the values are wall-clock ceilings, not token-progress claims. The 10 KB composition/repair took minutes and the larger run exceeded ten minutes, so the previous 75 s/120 s artifact and 60 s/90 s repair windows would have killed ordinary builds.
- Failed artifact revisions now retain bounded source HTML, exact UTF-8 bytes, and SHA-256 in both the in-memory and D1 stores. The rejected 40,357-byte artifact, its 483-byte repair fragment, and the first failed revision of the 10 KB run are therefore inspectable. This satisfies PRD US-05 and `ACCEPTANCE.md` B-US05's two-revisions requirement; no failure path renders the raw source as a same-origin page.
- Milestone-3 UX work is implemented: the owner chrome is styled as a deliberate deadpan surface, the sandbox frame is full width with a 390 px minimum mobile height, and the keeper spec is available as a safe view plus text download. Accepted HTML is a download-only attachment with `Content-Disposition` and `nosniff`; it is never rendered outside `/run`'s opaque sandbox.
- Owner-authorized embedding retest is enabled: the widget resource declares `frameDomains: [COMPANION_ORIGIN]`, `/c` and `/run` send the exact current ancestor allowlist `https://chatgpt.com https://chat.openai.com` (plus the companion origin for `/run`), and link-out remains available as fallback. The owner must record the observed ChatGPT ancestor/error result; no claim of embedding success is made here.
- Production redeploy (2026-07-18 15:56 JST, final source commit `b91d35d`): Wrangler authenticated on the owner account and deployed Worker version `95bc20fa-e53f-4187-a2e0-7000de3cb82d` to `https://randomware.randomware.workers.dev` on the free plan. After edge propagation, the initialize-first deployed e2e passed: MCP `2025-06-18` handshake, 8 tools, 10 full CallToolResult shape checks, widget `frameDomains`, responsive `/c` chrome and ancestor CSP, attachment-only accepted HTML download with `nosniff`, and keeper spec view/download. No signing secret was written to the repository.
- Widget creation-opening gap and fix (2026-07-18): external inspection found the prior 5,160-byte widget had no `/c/:id` URL, iframe, or `window.openai.openExternal` path. The widget now detects a completed/routable tool result, embeds the trusted `/c/:creationId` companion page in `#creation-frame`, persists the creation ID, and renders a visible “Download or open the creation” link. The link invokes `window.openai.openExternal({ href, redirectUrl: false })` when available and retains a normal browser link fallback. The deployed template is 6,889 bytes and contains all three paths; the production e2e asserts them. Worker version `0f475d99-6b1f-485a-b32c-3bd87a3a3fe5` passed after edge propagation. The owner may need to refresh or recreate the ChatGPT connector to fetch the updated resource template.
- Browser CSP regression and fix (2026-07-18): real-browser rendering confirmed that inline `/c/:id` chrome CSS was blocked by `style-src 'self'`, yielding unstyled chrome and a default 300×150 iframe; no inline chrome script was present, so no script migration was needed. The chrome CSS now lives at same-origin `/creation.css`; accepted `/c/:id` responses contain no inline `<style>` or `<script>`, keep `style-src 'self'` without `unsafe-inline`, and serve the stylesheet as a static asset. Chromium acceptance now asserts computed chrome border `2px` and a 564px frame at a 390px viewport. The production Chromium render and deployed e2e both passed against Worker version `938e8817-2ceb-40f8-a455-abe523e17b19`.

- Broker dispatch regression found after the audio feasibility failure (2026-07-18): the deployed `/run` harness posts to `/api/runtime/call`, but the deployed probe received 404 for both `OPTIONS` and `POST` with `Origin: null`; the prior deployed e2e never exercised this path, so it could pass while generated-app calls were dead and no `runtime_requests` rows were written. The Worker and local compatibility server now dispatch `/api/runtime/call` before other API/static fallthrough, allow only the sandbox's opaque `Origin: null`, answer the preflight with 204 and explicit POST/OPTIONS CORS headers, and retain the signed-capability broker plus D1 logging path. A Worker-shaped regression test proves preflight, a real signed broker POST, HTTP 200, and one new runtime request row. The deployed e2e now extracts the freshly minted `/run` capability, performs the same OPTIONS preflight and POST, and verifies the new D1-backed request row; deployment evidence follows this code change.
- Broker dispatch deployment evidence (2026-07-18): source commit `4fbdd3b` deployed as Worker version `983b6c0f-1ffc-481c-9e90-552ba894d2a2`. The first e2e invocation immediately after deploy saw the old edge response for OPTIONS; after propagation, the same command passed with `brokerPreflightStatus: 204`, `brokerPostStatus: 200`, and `runtimeRequests: 1`. The live test used the `/run`-embedded freshly minted capability, called the selected fixed operation through the broker, and observed the new D1-backed row. The owner can now retry the audio test; no runtime route was left untested by the production e2e.

- Signed media pipeline implementation (2026-07-18): the audio failure was traced to the absent §5.4 pipe: JSON worked, but raw Radio Browser `url_resolved` reached generated data and `/run` blocked it with `media-src`. Commit `6d5b663` added `media_tokens` D1 (migration applied remotely), short-lived HMAC media tokens carrying the exact resolved URL, strict credential/IP/loopback/local/scheme checks, manual two-hop redirects, five-minute/8 MiB/one-stream caps, and a same-origin `/media/:token` route. Radio Browser now returns bounded metadata without `url`/`url_resolved`; LibriVox resolves one RSS enclosure server-side and restricts it to archive.org (including validated `*.us.archive.org`) hosts. `/run` declares `media-src blob: <companion-origin>`. Final deployment after hardening was Worker `7f24582a-5688-41c9-bfd7-e06e5fe16d50`; deployed e2e passed with preflight 204, broker POST 200, and radio media 200 `audio/aacp` (3,355-byte first chunk). Live proof uses stable Radio Browser; the LibriVox signed archive path is covered by fixture and archive-policy integration tests and remains health-gated if dynamic RSS exceeds six seconds.

- Widget render regression investigation (2026-07-18): real ChatGPT evidence showed widget-originated `spin_apis` calls were reaching D1 and leaving runs at `spinned`, while the mounted widget stayed at its idle text. The renderer now consumes the real MCP `CallToolResult` envelope (including a JSON-RPC-wrapped result), ignores stale mount-only globals that lack run state, preserves the active widget run across `openai:set_globals`, renders sequential reels, concept acceptance, artifact/repair progress, honest failure, and the embedded creation transition, and has a browser acceptance fixture using the full `{content, structuredContent}` envelope. Model-initiated runs are intentionally not adopted into the widget without an active widget-owned run; they remain conversation/link-out runs, and this limitation is explicit rather than presenting a false idle-to-build state.

- Widget fix deployment evidence (2026-07-18): commit `39e99d7` deployed as Worker `5fbb0b6d-99d7-42ef-bd0d-35712127495d`. Local acceptance passed with 43 unit tests, 9 integration tests, and browser assertions for the real CallToolResult envelope, stale mount update, sequential reels, concept/artifact transitions, and embedded creation. The deployed resource read returned `text/html;profile=mcp-app`, the companion `frameDomains`, a 9,049-byte template containing `structuredContent`, `openai:set_globals`, repair-state copy, and `creation-frame`. The full production harness reached the new resource checks but stopped at its live Radio Browser specimen with `audio_stream_failed:radio-browser:400 application/json`; this is an upstream audio-selection failure, not a widget assertion failure. The owner should refresh or recreate the ChatGPT connector to pull the new template.

- Follow-up bridge/fallback deployment evidence (2026-07-18): commit `6714663` deployed as Worker `874c0dc3-d8fb-4e38-b821-04e2c18e98fd`. The build button now feature-detects and logs `window.openai.sendFollowUpMessage`, treats thrown or unsuccessful results as immediate UI errors, exposes a copyable prompt bound to the active `runId`, pauses the concept timer, and offers an explicit resume control; the existing choreography timeout remains terminal after resumption. Acceptance passed with 44 unit tests, 9 integration tests, and Chromium checks for missing, unsuccessful, and successful follow-up calls. The deployed resource read returned `text/html;profile=mcp-app`, the companion `frameDomains`, and a 11,973-byte template containing the follow-up API, fallback prompt, pause/resume state, and timeout behavior.
- Activity-aware choreography correction (2026-07-18, commit `dcbc1c8`): live run `run_7edaa4146ab78e7b6a881e7ed4aac896` showed that a 60/120-second concept ceiling killed an active rejection-and-resubmission loop. The server now persists a phase clock (`lastActivityAt`, inactivity deadline, absolute backstop) in the in-memory and D1 run snapshots. A tool call resets only the current phase's inactivity countdown—even a rejected `submit_concept`—while out-of-phase calls do not; phase transitions start fresh clocks, status GETs do not count, and absolute backstops are fixed at concept 10 minutes, artifact 20 minutes, and repair 20 minutes. The selected inactivity windows are concept 180/300 seconds and artifact/repair 300/600 seconds (first steer / post-steer). The widget polls `statusUrl` every three seconds and refreshes its timer from the server snapshot, so remount state cannot preserve stale absolute deadlines. Unit tests cover the fixed backstop and rejected-phase activity reset; Chromium acceptance covers a live status-poll refresh. Full `npm test` passed (46 unit, 9 integration, 1 containment/e2e), and browser acceptance passed (styled chrome, 564px frame, real CallToolResult envelope, follow-up fallback, and timer refresh). The verified source was pushed to `main` and deployed on the free plan as Worker version `2354cd0d-7642-4adc-be13-3ffd530b3162`; after edge propagation, `node scripts/test-deployed.js --base-url=https://randomware.randomware.workers.dev` passed with initialize-first MCP, 8 tools, 10 CallToolResult checks, broker preflight 204/POST 200, and one runtime request row. The deployed widget resource includes `statusUrl` and `setInterval` polling.
- Final repair-state redeploy (2026-07-18, commit `889401c`): the widget now clears stale artifact choreography metadata immediately when a rejected artifact enters `repair_requested`, then adopts the server's repair clock on the next status poll. Worker version `16f323e7-d4c9-42fd-bddf-46d83a4ce156` passed the deployed acceptance probe after edge propagation with the same MCP, tool-result, broker, and runtime-row evidence; the status-polling resource was verified at 14,560 bytes. The final local rerun passed `npm test` with 47 unit, 9 integration, and 1 containment/e2e test.
- Synthetic-model deploy gate and validator hardening (2026-07-18, source commit `a21d569`, gate wiring `c82b52a`): the concept validator now guards every nested access, emits field-specific diagnostics such as `api_role_operations_missing:open-meteo` or `api_role_operations_type:open-meteo`, and converts unexpected validation exceptions to logged `internal_validation_error` results. Both Worker and compatibility-server MCP surfaces now publish the full nested §4.2 schemas for all eight tools; run/prompt/artifact contract fields are described explicitly. `scripts/test-synthetic-deployed.js` runs initialize → spin → concept → artifact → status, then fuzzes every required field once missing and once mistyped. Against production Worker `a6c9d2ba-96f1-44e0-b1ce-983f25922589`, run `run_8cec429fcb9f752675525464dccc9473` completed and all 132/132 fuzz cases returned structured 4xx field-named errors with no raw `TypeError`/property-access strings. The driver is wired first into `test:e2e:deployed` and exposed as `test:e2e:synthetic`; a later combined e2e reached its existing live Radio Browser media specimen but received an upstream 400, while the synthetic deploy gate remained green and was rerun independently after edge propagation.

## 2026-07-18 — Sol escalation and full contract-coherence pass

- Owner-directed model escalation: the same primary implementation session switched from GPT-5.6 Luna to GPT-5.6 Sol at high reasoning effort. Reason: the Luna implementation produced 13+ defects reproduced by real clients, including three instances of the same schema/validator-drift class. The switch is recorded without inventing a usage meter; `BUDGET.md` requires a later owner-transcribed checkpoint.
- The immediate third drift instance was `dependency.to`: production validation accepted only `api_input | rules | interface_state`, but the deployed `submit_concept` schema exposed an unconstrained string. A model therefore had no visible route to a valid value and repeated `dependency_to_invalid`. This pass treats that as a contract-system failure, not a one-field patch.

Field-by-field drift audit, starting from `ARCHITECTURE.md` §4.2/§4.3:

| Contract field/rule | Validator before pass | Deployed schema/prompt before pass | Coherent projection after pass |
|---|---|---|---|
| Common `requestId`, `runId`, `runContract`, `promptVersion` | Tool layer required strings | Required fields existed; source/copy semantics were incomplete | Required fields, bounded IDs, descriptions, examples/source annotations, and prompt manifest come from one definition |
| `appName` | 4–48 trimmed chars; banned phrase/novelty checks | Plain string; no bound | `minLength: 4`, `maxLength: 48`, 2–4-word semantic rule, banned/novelty rules in schema + prompts; validator reads the same bounds |
| `premise` / `playerAction` / `noveltyDelta` | 20–180 / 20–180 / 8–180 chars | Plain strings | Exact length bounds generated into schema/prompt and consumed by validator |
| `apiIds` | Exact selected-ID coverage and nonempty strings | Array of strings, no cardinality/coverage guidance | 2–3 unique IDs, exact-coverage/source metadata, and semantic rule are visible; exact selection remains server-enforced |
| `causalChain[]` | One item per selected API; integer order; action 8–120 | Nested required fields only | Cardinality, one-based order range, action bounds, exact-coverage rule, examples, and selected-ID source are visible and shared |
| `apiRoles[]` | Exact API coverage; essential role 15–180; operations nonempty and allowed | Nested fields required, but no bounds/cardinality/source rule | Bounds, nonempty unique operations, exact coverage, selected operation source, and allowed-operation semantic rule generated from the contract |
| `dependency.to` | Enum `api_input | rules | interface_state` | Unconstrained string — owner-reproduced loop | Exact enum generated into `tools/list`, every prompt manifest, and validator; invalid enum fuzz must return `arguments_dependency_to_enum` |
| Other dependency fields | Selected `fromApiId`; optional selected `toApiId`; explanation 1–240 | Types/required only | Selected-ID source/semantic rule and exact explanation bounds generated and enforced |
| `interaction` | 1–4 controls; nonempty control strings; outcome 8–180 | Types/required only | `minItems: 1`, `maxItems: 4`, item minimum, outcome bounds, examples, prompt manifest, and shared validation |
| `visualDirection` | Four required strings, each 4–100 | Required strings only | All four length bounds and extreme/non-SaaS instruction generated into schema/prompt and shared validation |
| `bannedShapeAssessment` | Six fields must be literal `false`; explanation 12–240 | Boolean types only | Six `const: false` declarations, explanation bounds, phrase list, and semantic rule generated into schema/prompt and validation |
| Artifact `html` | 10,000–40,000 UTF-8 bytes; ≤2,000 nodes; document/viewport/control/marker/ready/call/policy checks | Plain string; only prose carried some literals | `x-randomware-utf8-bytes`, node cap, required literals, and semantic rules generated into schema + every prompt; validator and fixture generator consume the same values |
| `declaredApiUses[]` | Exact selected API/operation use is expected by §4.3/static validation | Nested fields required, no cardinality/source constraints | 2–3 exact-coverage declarations with nonempty unique selected operations and source annotations |
| Repair fields | `failedRevisionId` and diagnostics required by repair transaction | Required types only | Required fields plus nonempty diagnostic-code list/source descriptions generated; full diagnostics and the full contract remain in repair prompts |
| Capability/runtime contract | 10-minute token; 30 JSON calls; 2 concurrent; 1 MiB adapted; media 5 minutes/8 MiB/one stream/two redirects | Not visible in input schemas or full prompt contract; defaults repeated in signer | One capability definition feeds signer defaults, schema `x-randomware-runtime`, and prompt manifest |
| Control tools | Required/type checks only | No enums or useful examples | `record_choreography_failure` publishes the concept/artifact/repair phase and `choreography_timeout` enums; other control fields carry bounded examples/source descriptions |

- The shared definition now generates all eight input schemas and a compact `RANDOMWARE_CONTRACT_JSON` manifest carried by initialize instructions, every tool description, accepted-concept guidance, widget build/repair prompts, and exact repair diagnostics. Concept/artifact validators, the default artifact generator, and capability signer consume the same bounds/literals/defaults. A unit contract test proves enum/range/const enforcement and fails if the production synthetic driver imports any `src/` contract or artifact helper.
- The upgraded synthetic driver constructs values solely from deployed `tools/list` schemas, prompt manifests, and selected API results. It compares the deployed schemas to the deployed prompt manifest, walks required/type/enum/const/range/custom artifact/capability constraints, generates a complete artifact from visible literals, fuzzes every required field missing/mistyped plus every enum/const/range, and rejects raw exception text. It no longer imports the server schemas, validator constants, or fixture artifact generator it is meant to test.
- Pre-deployment evidence: `npm run acceptance:machine` passed 56 unit, 9 integration, and 1 containment/e2e test plus browser-rendered widget/chrome checks and every static/document gate. Against a migrated local Worker after the final policy projection, the upgraded driver completed run `run_bc9675c998ec646aed8038ebea2040c3` with the exact three-way selection `radio-browser + nager-date + librivox`, accepted creation `creation_afc0be45941a178ce2d86bb6216a5fc9`, streamed a signed same-origin Radio Browser response (`audio/mpeg`, HTTP 200, 26-byte first chunk), checked 145 deployed schema constraints, and passed 247 structured negative cases including 3 enum, 6 const, and 106 range cases.
- Deferred Milestone-3 batch — recorded only, not implemented in this pass: steer default artifacts to 10–16 KiB with an explicit compact-code instruction to target owner-observed ≤5-minute builds; polish slot/progress animation; and keep the connector-cache operational note prominent. After a widget-template deployment, refresh the ChatGPT connector (sometimes twice) or recreate it if a stale-template “Runtime error” remains; the owner hit this exact cache state today.
- Production deployment and gate evidence: source commit `cea78ba` deployed to Cloudflare Workers Free as version `d5486058-0024-4392-8f93-b488d9ab391f`. The first forced-trio attempt correctly stopped because production health still marked LibriVox degraded, making the required selection ineligible. A fresh bounded 18-entry verification then measured 15 healthy entries—including Radio Browser, Nager.Date, and LibriVox—and published those exact statuses to production D1 (18 rows written); no trial-spin spray was used to discover the health-dependent seed.
- The upgraded production synthetic gate then passed twice. Standalone run `run_a028c6939707524192023b5677acd135` selected exactly `radio-browser + nager-date + librivox`, accepted audio-bearing creation `creation_7aa1db54a93dae54c2d8c6c6e15991ad`, and streamed a signed same-origin Radio Browser response (HTTP 200, `audio/mpeg`, 2,014-byte first chunk). The full deployed e2e independently repeated the exact selection in run `run_778214f101fa13f7ef08cfaccbcb9ec4`, accepted creation `creation_6e14e75e97a6aaa076de97b4fd174ebe`, passed all 247 structured fuzz cases (3 enum, 6 const, 106 range), checked 145 schema constraints and 12 prompt surfaces, and streamed signed audio (HTTP 200, `audio/mpeg`, 2,048-byte first chunk). The surrounding deployed suite also passed MCP `2025-06-18`, all 8 tools/10 CallToolResult checks, widget resource syntax, broker opaque-origin preflight 204, broker POST 200 plus a new D1 runtime row, keeper/repair/failure surfaces, and a separate Radio Browser stream (HTTP 200, `audio/mpeg`, 13,592 bytes read). This is the required green production gate before owner testing.

## 2026-07-18 — Cross-origin widget status polling repair

- External reproduction against completed run `run_6ef6b796de650409e989c491d3ba1ffa` confirmed that `GET /api/runs/:id` returned 200 without `Access-Control-Allow-Origin`, so ChatGPT's sandbox-origin widget could not observe completion and its empty `catch` hid every failure. Source commit `8d9a231` adds credential-free `Access-Control-Allow-Origin: *` only to the public run-status read model, answers its OPTIONS preflight with 204 and `GET, OPTIONS`, and leaves write-route CORS unchanged. The widget now logs polling failures, resets the count after a successful status read, and displays the exact visible line `status polling unavailable` after three consecutive failures.
- The machine gate passed 56 unit, 10 integration, and 1 containment/e2e test plus the browser-rendered acceptance. Chromium forced three consecutive failed status fetches and observed the visible line before continuing through concept, completion, and embedded creation; the owner chrome remained styled and the frame measured 564 px high at a 390 px viewport.
- Worker version `77f2bfdf-07c5-4636-96e4-9ab72ac1e999` is deployed at `https://randomware.randomware.workers.dev`. The production gate passed with foreign-origin run-status GET (`Access-Control-Allow-Origin: *`), OPTIONS 204, the exact `radio-browser + nager-date + librivox` synthetic choreography, 247 negative fuzz cases, 145 schema constraints, and signed Radio Browser audio (`audio/mpeg`, HTTP 200/206). A separate foreign-origin probe of the owner run returned the same GET and OPTIONS headers. Because the polling behavior changes the widget template, the owner must refresh the ChatGPT connector again before the final confirmation round.

## 2026-07-18 — Canonical widget URLs and signed image assets

- Root cause and URL repair: every run-shaped tool result and public status payload now carries an absolute HTTPS `statusUrl` on the canonical companion origin and a nullable absolute `creationUrl`; completed/failed creations always carry the latter. The widget fetches returned status URLs verbatim, embeds/opens returned creation URLs verbatim, and uses the shared companion-origin constant only as a fallback. It no longer resolves either path against the ChatGPT sandbox's `window.location`. The foreign-origin Chromium fixture proves that polling requests, iframe `src`, anchor `href`, and `openExternal({href})` all target `https://randomware.randomware.workers.dev`, while the deployed gate rejects relative or off-origin URLs in tool/status results.
- Signed asset mediation: migration `0004_asset_tokens.sql` adds page-load and one-use asset-token accounting. Every image-bearing launch operation has explicit field paths and bounded hosts: Deck of Cards, ArtIC IIIF, Dog CEO, RandomUser, Wikipedia On This Day, Met Museum, TVMaze, Rick and Morty, Open Food Facts, and TheMealDB. The broker stores the exact validated HTTPS upstream URL, rewrites the same output field in place to a short-lived signed `/api/runtime/asset/:token` URL, and emits no raw image URL when signing context is absent. The asset route revalidates the token, creation/revision/API/operation/page nonce, exact URL and registry host policy; follows at most two revalidated redirects; permits only image MIME types; enforces 2 MiB per asset and 8 MiB per page load; and serves with `nosniff`, no-store, a sandbox CSP, and cross-origin resource permission required by the opaque `/run` sandbox. `/run` keeps `img-src data: blob: 'self'` unchanged.
- Gate coverage: the offline suite rewrites every configured image field for all ten image-bearing operations, asserts no upstream image host survives, and validates an image content type under each operation's exact policy. Worker integration proves a real signed route plus page quota. The deployed suite chooses a currently healthy image-bearing operation, requires a same-origin signed URL, rejects raw image URLs while preserving legitimate provider/terms links, and streams the signed asset. Live ArtIC IIIF currently presents a Cloudflare browser challenge to server-side clients; the ArtIC operation therefore requests its API-provided bounded LQIP thumbnail, removes the alternate raw thumbnail field, and serves the exact image payload through the same signed token and quota route. Production proof against an existing ArtIC creation returned broker 200 and signed asset 200 `image/gif` (162 bytes), with no IIIF fetch or CSP bypass.
- Audio gate hardening discovered during this verification: a syntactically valid Radio Browser candidate can still be dead. The live adapter now probes the eight already-bounded candidates sequentially with a 2.5-second range request, validates redirects and audio MIME, cancels the probe body, and signs only the first streamable exact resolved URL. The fixture path remains deterministic. A regression test proves that a 400 first candidate is skipped for a valid second candidate.
- Verification and deployment: `npm run acceptance:machine` passed 64 unit, 11 integration, and 1 containment/e2e test plus the foreign-origin 390 px Chromium fixture, build, registry, security, secrets, and document gates. Remote D1 migration `0004_asset_tokens.sql` applied successfully. Source commits `f7d240c`, `833d24f`, `1bbbe12`, `6437ecc`, `d375976`, `10dae2b`, `40233f1`, and `e891914` are pushed to `main`; final Worker version `22e50201-08a8-4e44-94cb-a0126db76d00` is deployed.
- Final production gate: synthetic run `run_e2af0c02f89954800eecfc1cc52cb50a` selected exactly `radio-browser + nager-date + librivox`, accepted creation `creation_7f257eb407380d34374aa37c8078fc96`, checked all 8 tools, 12 prompt surfaces, 145 schema constraints, and 247 negative cases (3 enum, 6 const, 106 range), then streamed signed Radio Browser audio (`audio/mpeg`, HTTP 200, 3,471-byte first chunk). The surrounding e2e passed MCP `2025-06-18`, foreign-origin status GET/OPTIONS, opaque-origin broker preflight/POST plus D1 row, a second live signed Radio Browser stream (`audio/mpeg`, HTTP 200, 5,715-byte first chunk), and a live signed Dog CEO image (`image/jpeg`, HTTP 200, 28,245 bytes). This is the requested green production gate. Because the URL repair changes the widget template, the owner must refresh the ChatGPT connector (sometimes twice) or recreate it before the final embed/image/audio confirmation.

## 2026-07-18 — Browser-playable radio preference and signed-media CORS

- Owner replay of **Atmospheric Dial** proved that signed media minting and streaming were healthy (`/media` returned HTTP 200 `audio/aacp`), but the upstream-first adapter selected EXA FM's `AAC+` codec. Chrome's native audio element could not decode it and emitted an error. The adapter now stable-partitions the eight Radio Browser candidates: `MP3`, `MPEG`, `audio/mpeg`, and plain `AAC` spellings are tried first in upstream order; `AAC+`/`AACP`, OGG/Opus, and unknown codecs remain ordered fallbacks rather than becoming a hard failure. Existing URL validation, live stream probe, redirect policy, and exact-URL signing remain unchanged. Tests prove that an earlier valid AAC+ station loses to a later MP3 station, that all-low-priority input still returns the first valid fallback, and that the committed fixture corpus defaults to a preferred codec.
- The signed `/media/:token` read route now answers OPTIONS with 204 and publishes `Access-Control-Allow-Origin: *`, `GET, OPTIONS`, Range request permission, and exposed range/content headers. GET responses carry the same wildcard read CORS, allowing both native `<audio src>` and opaque-sandbox `fetch` → blob players. Capability validation, one-stream/five-minute/8 MiB limits, upstream mediation, and `/run` `media-src` CSP are unchanged. The compatibility server mirrors the Worker behavior.
- Verification and deployment: source commit `ed9358e` is pushed to `main` and deployed as Worker version `a0366194-a032-411f-9212-f68c84e1a9cb`. `npm run acceptance:machine` passed 66 unit, 11 integration, and 1 containment/e2e test plus browser, build, registry, security, secrets, and document gates. The full production gate passed: foreign-origin media preflight 204, media GET `Access-Control-Allow-Origin: *`, live `audio/mpeg` (1,754-byte first chunk), signed Dog CEO image 200, and synthetic run `run_d78c6f653dd3ad318f1d7e239c19284e` / creation `creation_a0b5e85dcbde068af69e6dd214b9fee7` with the exact Radio Browser + Nager.Date + LibriVox selection, 247 fuzz cases, 145 schema constraints, and a second `audio/mpeg` stream (5,930-byte first chunk).
- Still owner-verifiable, not claimed complete from server-side evidence: **(a)** the absolute-URL widget path actually renders the creation iframe inside a real ChatGPT widget, and **(b)** a default Radio Browser draw actually produces audible playback in that real widget/browser. The server-side absolute URLs, codec priority, media CORS, and audio bytes are proven; neither owner-observed UI outcome is inferred from them.

## 2026-07-18 — Runtime data-contract coherence

- External replay showed that healthy broker/asset/MCP calls were not enough: generated artifacts had never been told that `window.randomware.call` returns an envelope and that the application payload is exactly `result.data`. **Seismic Weather Oracle** read `result.current`/`result.features`; **Bark Exchange** unwrapped `.data` but followed the stale Frankfurter v1 fixture's `rates.JPY` instead of the live v2 flat `rate`. These and all other already accepted pre-fix specimens remain immutable frozen artifacts; they predate the runtime data contract and are not patched in storage.
- The owner-curated adapted goldens are now the executable source for every operation's bounded `responseExample`, structural `outputSchema`, deterministic key/container signature, and semantic leaf paths. Scalar/null values intentionally share a leaf signature so random provider content does not create false drift; field additions/removals and object/array changes still fail. The fixed broker envelope, adapted-shape rule, signed image/media semantics, and selected operation examples are projected into `RANDOMWARE_CONTRACT_JSON`, initialize/tool code guidance, accepted-concept guidance, widget build prompts, and full repair prompts. New artifacts are explicitly instructed to use `result.data`, same-origin signed image fields verbatim, and signed `/media` URLs only.
- All 18 adapted goldens were recaptured through the production adapters from their fixed live endpoints on 2026-07-18. Frankfurter now records `{date, base, quote, rate}`. The previously invalid Wikipedia path was corrected to the bounded English On This Day events route; the Met probe now uses public-domain object `436121`; Wikipedia's raw cap is 512 KiB before its three-event adapter/64 KiB output cap. Source preparation fixtures under `samples/` were not overwritten.
- Idempotent GET operations now retry exactly once only after `runtime_timeout`. The retry is written as `runtime_timeout_retry` in `runtime_requests` before the final success/failure; successful-call quota accounting and the public broker envelope are unchanged.
- Gate closure is semantic rather than HTTP-only: the deployed synthetic artifact derives paths solely from model-visible selected operation contracts, unwraps `result.data`, renders one non-default live field per selected operation, and the production driver verifies the same fields. Local Chromium now clicks the accepted artifact at 390 px and observed non-default USGS, Rick and Morty, and LibriVox values inside the sandbox. The local compatibility server's `/run` `frame-ancestors` now uses the request origin instead of incorrectly forcing `https:` during loopback acceptance; production HTTPS policy is unchanged.
- Independent pre-deploy review found three Important gate holes and one documentation ambiguity. First, the hourly cron health path could overwrite manual shape-drift quarantine; it now runs the same adapted signature comparison before publishing D1 health. Second, fixture mode still replayed stale raw sample shapes (11/18 mismatched the new visible contract); the broker now conforms raw adapter output to the committed adapted key/container shape while retaining available signed asset/media rewrites, and a complete 18-operation fixture/visible-contract parity test passes. Third, production synthetic checked live fields directly without executing the submitted artifact; the deployed e2e now invokes Chromium against the production companion, clicks the generated artifact, and requires non-default semantic rendering inside `/run`. Finally, §4.5 now distinguishes prohibited live/raw response injection from the explicitly owner-curated bounded adapted examples. The reviewer requested these fixes before deployment; no Critical findings were reported.
- The narrow re-review found two further Important containment-contract holes before deployment. Hourly health had no signing context, so otherwise healthy Radio Browser and LibriVox responses omitted the contract-required `mediaUrl` and would be degraded by the new shape check; health now supplies bounded no-op asset/media signing stores and tests the audio shape. Fixture conformance could also copy a committed golden asset placeholder when a stale raw sample lacked the corresponding field. Adapted goldens now retain internal audited `fixtureSources` metadata outside their model-visible `data`; fixture fallback mints a fresh active-origin signed URL from that exact source, never publishes a `randomware.example` placeholder, and is covered with Deck of Cards' deliberately thin raw fixture. These internal source URLs are not emitted in prompt examples or public broker results.
- Final independent re-review of commit `58c663d` returned **Ready** with no remaining Critical or Important findings. The reviewer reproduced a healthy signed Radio Browser health response, checked all 18 fixture operations for active-origin mediated asset/media URLs and corresponding token records, and confirmed that neither golden placeholders nor internal `fixtureSources` enter model-visible contracts.
- The first post-deploy live shape gate then caught two nondeterministic provider-owned shapes before health publication: LibriVox conditionally omitted optional book metadata, and Met exposed variable nested measurement keys. Both adapters now emit fixed-field summaries with explicit nullable scalars; LibriVox authors likewise have fixed nullable fields. Met measurements are intentionally excluded. The two adapted goldens were recaptured live, response contracts regenerated, and machine acceptance passed with 79 unit, 12 integration, one containment test, and Chromium rendering non-default Met and LibriVox semantic values. The failed initial deployed gate (`three_api_audio_selection_failed`) made no choreography mutation beyond its bounded spin attempts and was caused by pre-existing degraded production health rows, not by weakening the selector.
- Final production handoff (2026-07-19 JST): the owner accepted the remaining LibriVox result as a temporary provider issue. Repeated bounded probes returned no bytes within the six-second launch policy (and the hourly health row independently recorded `runtime_timeout`), so no false healthy status was published and the latency contract was not loosened. All non-provider verification remains green: the clean-tree `acceptance:machine` run passed 79 unit, 12 integration, one containment test, and Chromium semantic rendering; production D1 contains an audited `radio-browser/station` `runtime_timeout_retry` row; and the post-deploy smoke returned `/healthz` 200 plus MCP `2025-06-18` with tool/resource capabilities. Commit `ea2029d` is deployed on Workers Free as version `261d7a21-3558-4f3c-b4c6-6d1f686200f0` at `https://randomware.randomware.workers.dev`. No stored pre-fix artifact was modified.
- Owner-transcribed usage memo (2026-07-19 JST): the weekly plan meter is **0% remaining**; grant credit balance is **2,111/2,500** (389 credits consumed since the prior 2,500 snapshot). Per `BUDGET.md` §4/§6, this closes non-reserve work and optional iteration; only reserve-eligible acceptance, security, deployment, or required submission evidence may proceed.
- Audio API recovery/publication (2026-07-19 JST): the two existing audio sources were rechecked and applied through the normal health path. Radio Browser returned HTTP 200 in 3,048 ms; LibriVox returned HTTP 200 in 5,535 ms; both adapted shapes matched their goldens. `registry:verify:live` passed all 18/18 entries, and `registry:health:publish` wrote 18 healthy rows to production D1. No new TTS/STT API was added; the project’s existing Radio Browser + LibriVox pair is the two-source audio surface.

## 2026-07-19 — Real ancestor evidence and partial runtime degradation

- The previously unverified real-widget ancestor question is now closed on the failure side: the owner observed the embedded `/c` document refuse to connect. The deployed CSP admitted only `chatgpt.com` and `chat.openai.com`, while ChatGPT placed the widget inside a connector-specific `https://<connector>.web-sandbox.oaiusercontent.com` iframe; because CSP `frame-ancestors` must match every ancestor, the intermediate sandbox blocked both `/c` and nested `/run`. Both policies now retain the existing chat and companion entries and add the bounded apex plus subdomain sources `https://web-sandbox.oaiusercontent.com https://*.web-sandbox.oaiusercontent.com`. Post-fix header verification is automated; a successful visual embed remains pending owner observation in a refreshed/recreated connector and will not be inferred from headers alone.
- Live rows for `run_b79b31585eb3d71d2918220da0ce2aa2` showed the first LibriVox click timing out twice under the six-second cap while TheMealDB and ArtIC succeeded; a minute later LibriVox returned 950 bytes and remained warm. The fixed LibriVox operation and its health latency limit are now 10,000 ms, including RSS audio resolution, while the existing single timeout retry and 30-second response cache remain bounded.
- Runtime-data guidance now projects one shared degradation rule into `RANDOMWARE_CONTRACT_JSON`, initialize/tool guidance, accepted-concept guidance, widget build prompts, and repair prompts: use `Promise.allSettled` or equivalent, render fulfilled adapted results, and show an honest per-source failure line for each unavailable source instead of blanking the entire app. The concept validator still requires every selected API to be essential. Default, synthetic, and browser-acceptance artifacts follow the same rule.
- Pre-deploy machine acceptance passed 82 unit tests, 12 integration tests, one containment/e2e test, and Chromium at 390 CSS pixels (styled 564 px frame with non-default semantic Deck of Cards and ArtIC values), followed by build, 18-entry registry, security, secrets, and document gates.
- Source commit `fdb0569` is pushed to `main` and deployed on Workers Free as version `67c47985-672a-4f1c-9fc3-126766fb42b2`. Post-deploy header captures for synthetic creation `creation_fed64e432749b73045caa35a95439889` show `/c` with `frame-ancestors https://chatgpt.com https://chat.openai.com https://web-sandbox.oaiusercontent.com https://*.web-sandbox.oaiusercontent.com`; `/run` carries the same four sources plus `https://randomware.randomware.workers.dev`. This proves the deployed policy, not the owner-visible render; successful real-ChatGPT embedding remains explicitly pending a refreshed/recreated connector observation.
- The post-deploy bounded registry run passed 18/18 live operations with 18/18 adapted-shape matches and published 18 healthy rows to production D1. The full deployed gate passed MCP `2025-06-18`, all 8 tools, both ancestor assertions, foreign-origin status CORS, opaque-origin broker preflight/POST with D1 logging, signed Rick and Morty image (`image/jpeg`, 39,323 bytes), signed Radio Browser audio (`audio/aac`), and Chromium semantic rendering at 564 px. Its source-visible synthetic choreography selected exactly `radio-browser + nager-date + librivox`, rendered live values `Real 93.1`, `元日`, and `Count of Monte Cristo`, passed 247 structured fuzz cases/145 schema constraints/12 prompt surfaces, and streamed a second signed audio response. No whole-app blanking was accepted by the gate.

## 2026-07-19 — Signed-media browser reconnect repair

- Owner production evidence for `creation_88d342dc16e08e2719bc29e4862c4ed4` (Radio Browser KNBA MP3) isolated the failure to stream-slot accounting: a fresh signed URL streamed through curl, but Chrome's normal media loader opened and aborted one connection before reconnecting. The reconnect then received `media_concurrent_limit`; production D1 showed multiple tokens stuck with `active_stream=1` and `bytes_served=0`. The upstream stream, token minting, MIME, and media CORS paths were otherwise healthy.
- Root cause: media tokens used a boolean active flag that was cleared only after graceful completion. Cancellation cleanup was not durably attached to the Worker request context, and the compatibility pump lacked a guaranteed finalizer. A client abort could therefore strand the one-stream slot permanently.
- The media-token record now carries a per-connection lease. A new request uses last-connection-wins semantics, replacing a stale lease immediately; completion, cancellation, abort, and fetch failure all account bytes, while a late cleanup may clear the slot only if its lease is still current. The Worker schedules cleanup through `ctx.waitUntil`, the bounded stream runs cleanup from cancel/abort paths even if upstream cancellation throws, and the compatibility pump uses `finally`. Migration `0005_media_stream_leases.sql` adds the lease column. The existing five-minute, 8 MiB, redirect, URL, and MIME limits are unchanged.
- The deployed gate now reproduces the browser connection pattern: open a signed stream, abort after a chunk, reconnect to the same token with `Range`, and require an audio-capable 200/206 response. Local Chromium additionally loads the signed stream through a native `<audio>` element and requires `currentTime > 0` or `readyState >= 3`; the pre-deploy run reached `readyState=4`, kept the controls visible and unobstructed, and rendered the 390 px owner surface at 564 px frame height.
- Prompt guidance now requires native audio controls to remain fully visible and unobstructed by labels, overlays, and decoration. **Orbit Choir** and other accepted artifacts remain frozen and are not patched in storage; the guidance applies to newly generated specimens.
- Pre-deploy `acceptance:machine` passed 84 unit tests, 13 integration tests, one containment/e2e test, the signed-audio Chromium assertion above, build, all 18 registry entries, security, secrets, and document gates.
- Source commit `a0abe96` is pushed to `main`. Remote D1 migration `0005_media_stream_leases.sql` applied successfully before Worker version `0ade1f89-0e24-41b3-888b-b9a2753fa43f` deployed to `https://randomware.randomware.workers.dev`.
- Production replay on creation `creation_0adccf2af154122f9a0394200a890e31` exercised the required browser pattern with a fresh Radio Browser capability: broker 200; initial media 200 `audio/mpeg` with a 3,303-byte chunk; client abort; same-token reconnect with `Range` returned 200 `audio/mpeg`, CORS `*`, and a 3,111-byte chunk. This closes the fatal `media_concurrent_limit` behavior: a stale holder no longer prevents the replacement connection. A disconnect whose cleanup callback is not delivered may still leave the diagnostic active flag until the next request; last-connection-wins preemption is the deliberate self-healing path and a late old cleanup cannot clear the replacement.
- Gate scope is split according to the owner directive: deterministic signed-stream readiness and unobstructed controls run in local Chromium; production e2e performs the live open-abort-Range-reconnect. A particular live station delivered replayable `audio/mpeg` bytes but did not reach Chromium readiness within 15 seconds, so live-provider decode is not substituted for the deterministic local playback assertion.
- The comprehensive production gate remains honestly blocked outside this repair: after live verification republished LibriVox healthy, two bounded exact-trio attempts still ended at `semantic_broker_status:librivox/book:400` with `runtime_timeout`. The measured live registry snapshot was 16/18 healthy (LibriVox and Radio Browser healthy; PoetryDB timed out and Open Food Facts failed upstream). No timeout was loosened and no false healthy semantic result was asserted. The reconnect production replay above and the complete local gate are green; a full all-provider production pass remains pending LibriVox recovery.

## 2026-07-19 — Owner audio observations and open UX questions

- Owner replay on the current deployment: a signed radio stream is audible when its media URL is opened by direct page navigation, but the same creation appears to produce no audible playback when run inside ChatGPT. This matches the deployed `/media` responses still carrying `cross-origin-resource-policy: same-origin`: a top-level navigation is a same-origin load and passes, while the opaque-origin `/run` sandbox makes every embedded audio load cross-origin, so the browser discards the response body (the signed asset route already serves `cross-origin-resource-policy: cross-origin` for exactly this reason). The one-header correction to the media route is directed and pending deployment; in-widget playback must then be re-verified in a refreshed/recreated connector, and no in-widget conclusion — success or failure — is recorded until that observation exists.
- Owner feedback on audio stability: upstream provider latency delays the first audible bytes, so playback can start noticeably after the interaction and read as broken before it begins. How the owner surfaces should treat this honestly — for example an explicit warming/buffering state around the native controls, deferred control enablement until the stream is ready, or narrower audio expectations in the concept guidance — is recorded as an open UX design question for a future round. No design decision is made in this entry, and no fake-progress treatment will be accepted as the answer.

## 2026-07-19 — Media embedding policy and round-4 registry evidence

- The owner confirmed two independent production audio blockers. First, signed `/media` responses still carried `Cross-Origin-Resource-Policy: same-origin`, so audio worked only by top-level navigation and was discarded inside the opaque-origin `/run` sandbox. Worker and compatibility-server media responses now use `cross-origin` for both 200 and 206 while retaining wildcard read CORS, signed-token validation, and all stream caps.
- Second, LibriVox minting accepted `https://www.archive.org/...`, but serving rejected that same host and allowed redirects only under `*.us.archive.org`. Mint and serve now use the same `validateMediaUrl` policy. The bounded LibriVox exception admits the `archive.org` apex and any proper `*.archive.org` subdomain while continuing to reject credentials, IP literals, local/private hosts, non-HTTP(S) schemes, and more than two redirects. Regression coverage reproduces `www.archive.org` → `archive.org` → `dn720708.ca.archive.org` → `audio/mpeg`.
- Round-4 candidate capture used one 10-second/200,000-byte probe per candidate with no retry. NASA Image and Video Library passed at HTTP 200, 4,289 bytes, 619 ms; its adapter ignores the insecure collection link and exposes only HTTPS `images-assets.nasa.gov` imagery. Library of Congress Photos passed at HTTP 200, 72,251 bytes, 1,725 ms; the captured fixture proved `results.*.image_url.*` on `tile.loc.gov`, so no guessed `cdn.loc.gov` permission was added. Its fixed `c=2` operation uses a five-minute cache and 240/day budget, well below the official 20 requests/minute guidance.
- GBIF returned no response within the single 10-second bounded probe. Per the owner’s cost and evidence constraints it was not retried, no fixture was invented, and it was rejected. iNaturalist remains owner-excluded after a 51.6 MB / 8.1-second raw observations measurement. The launch registry therefore grows from 18 to an honest 20 entries rather than a fabricated 21; NASA and LOC include raw fixtures, adapted goldens, generated response examples/output schemas, exact asset policies, policy metadata, symbols, selector/health integration, and fixture parity tests.
- Pre-deploy verification: the 20-entry offline registry and 96 unit tests passed. Local Chromium loaded the signed stream inside the sandboxed `/run` frame—not by top-level navigation—and observed `audio.currentTime=0.010576`, `readyState=4`, with visible unobstructed controls. The bounded live registry pass checked each of the 20 fixed operations once: 18 were healthy with matching adapted shapes. Dog CEO was unavailable after 7,373 ms and TVMaze was disabled for adapted-shape drift; neither was retried or patched in this round, and both remain unselectable through the existing health mechanism.
- The final pre-deploy `acceptance:machine` run passed 96 unit, 16 integration, one containment/e2e test, build, 20-entry registry, security, secrets, and document gates. Chromium again played signed audio inside `/run` (`currentTime=0.009218`, `readyState=4`) and kept native controls visible and unobstructed.
- The first production gate attempt reached successful live calls for Radio Browser, Nager.Date, and LibriVox, then stopped in the client harness: an assertion eagerly called `mediaResponse.text()` while formatting a success message, locking the same body before `getReader()`. The harness now reads an error body only on a non-200/206 response; a source regression assertion prevents this double-read. This was not a Worker/media failure and did not trigger an upstream retry.
- Production deployment: source commit `c2c30b4` was pushed to `main` and deployed on Workers Free as version `0f216f28-6abc-48d8-9de6-25dfcac0c9b2`. The 20-row live health report was published to D1 as 18 healthy, Dog CEO degraded, and TVMaze disabled; NASA and LOC are healthy.
- The corrected full deployed gate passed. The exact source-visible synthetic selection was Radio Browser + Nager.Date + LibriVox (`run_d9f3d00c25a4347aebeacd118a0abce2`); all three live semantic values rendered, 247 structured fuzz cases and 145 schema constraints passed, and the LibriVox signed URL served HTTP 206 `audio/mpeg` with 4,096 bytes and `Cross-Origin-Resource-Policy: cross-origin`. Separately, Radio Browser survived the browser-style open/abort/Range reconnect (200 then 206 `audio/mpeg`). Production Chromium played a signed stream inside the sandboxed `/run` frame with `currentTime=0.00988`, `readyState=3`, and visible unobstructed controls. This is embedded playback evidence, not a top-level-navigation substitute, so the registry-level audio-disable fallback was not activated.

## 2026-07-19 — RANDOMWARE.EXE chrome and owner-curated style deck

- Owner-authorized reserve work refreshed only the widget template, companion `/` and `/c` presentation, registry-backed display metadata, and the spin/concept style contract. Broker behavior, containment policy, and every stored artifact remained untouched. The widget is now one 390 px retro-OS window using the approved teal/grape/magenta/tangerine/cream token set, hard borders and shadows, CSS-only shuffle/crawl/blink/lamp motion with reduced-motion handling, a truthful four-step state display, server heartbeat, style cartridge, build timing, failure autopsy/retry actions, manual follow-up fallback, and the existing companion embed/link-out path.
- The public index and creation chrome now present as machine-issued paper records. The index keeps the judge bridge, embeds the first curated accepted specimen in one teal live-specimen machine window, and renders the curated paper card cabinet server-side. `/c` prints the record kicker, accepted/deceased stamp, owner symbols plus human API capability/category/docs, selected style cartridge, inspection actions, footer navigation, and strict-CSP external CSS; the frozen artifact iframe remains a separate unchanged sandbox below the record.
- `STYLE_DECK` is a deeply frozen owner-managed eight-entry source of truth: Paper Certificate, Video Game HUD, Flash App, Board Game, Gacha App, Retro 90s Pixel, Teletext Dispatch, and VHS Jacket. `spin_apis` draws one deterministic independent style while avoiding the session's three most recent IDs, persists `styleId`, returns the full cartridge, and requires exact `submit_concept.styleId` equality. The generated schema enum, validator, contract manifest, initialize/tool text, accepted-concept guidance, widget build/repair prompts, `/c`, and keeper spec consume the same entries. Existing accepted artifacts are unchanged.
- The first machine gate passed 101 unit, 16 integration, one containment/e2e test, build, 20-entry registry verification, security/secrets/document scans, and Chromium. At 390 px Chromium observed the reel shuffle, ordered stops, full-stop flash/lamp bank, style chip, `ELAPSED`/`AUTO-NUDGE`, failure panel/actions, 3 px paper chrome, 742.7 px mobile artifact frame, 792 px desktop frame, and a server-rendered index hero. The same local gate played deterministic signed audio inside `/run` (`currentTime=0.008515`, `readyState=4`) with visible unobstructed controls.
- A fresh read-only review found no Critical issue and two Important truthful-state issues: artifact-phase silence with no revision was labeled as a concept failure, and the pre-concept state said the model was composing an artifact. The fixes persist the failed choreography phase in D1, derive the failed step from it, and show phase-specific `DRAFTING CONCEPT`, `BUILDING SPECIMEN`, and `REPAIRING SPECIMEN` copy. Focused unit and Chromium regression checks passed, including an artifact-timeout BUILD-step assertion.
- No registry live probe was added in this UI/contract round. Per the owner's cost direction, no degraded or disabled provider was retried, and no provider status was inferred. Usage remains the last owner-transcribed snapshot: weekly plan **0% remaining**, grant balance **2,111/2,500**; it was not re-measured and no delta is invented. This round proceeds only under the owner's explicit required demo/acceptance reserve authorization.
- Source commits `108740d`, `85dba11`, and review-fix commit `3179c63` are pushed to `main`. Worker version `d084e73d-d00e-4325-a9ae-caaa3db08366` is deployed at `https://randomware.randomware.workers.dev`. The first immediate post-deploy read reached the prior MCP manifest during edge propagation and stopped before choreography; a subsequent bounded read showed both `styleHistory` and `styleId` with all eight enum values before the gate resumed.
- The final production UI/style gate passed all eight tools, MCP `2025-06-18`, the new widget template, absolute status/creation URLs, foreign-origin status CORS, broker preflight/POST plus a D1 request row, strict creation chrome, server-rendered hero, keeper/autopsy surfaces, and Chromium at 390 px. The deployed synthetic driver completed exact Radio Browser + Nager.Date + LibriVox choreography (`run_a9a72cc51b9c22035e9a0dd188c355fd`, creation `creation_e5520fd3e8499f25109a0dbd4dea1aa4`), checked 148 schema constraints and 251 structured negative cases, explicitly rejected the invalid `styleId` enum, rendered live semantic values from all three sources, and streamed signed LibriVox audio as HTTP 206 `audio/mpeg` (4,096-byte first chunk). A separate deterministic healthy Radio Browser draw passed signed-media preflight, 200 stream, abort, and 206 Range reconnect (`audio/aac`); Rick and Morty served a signed 39,323-byte JPEG.
- Production Chromium audio was not repeated to a false success in this UI-only round: one healthy Radio Browser selection did not advance playback within the existing 15-second observation window. Per the owner's skip-broken-provider direction, no station retry spray or health rewrite followed. The final deployed Chromium pass therefore scoped audio out while retaining the successful local sandbox playback evidence and both production signed-media byte paths above; it verified the requested widget stepper/reels/style/build/failure states, paper record, 742.7 px mobile frame, 792 px desktop frame, and live index hero. This does not revoke or replace the prior production in-frame audio evidence, and it does not claim that the newly drawn station was audible.

## 2026-07-19 — Audio roster replacement: Wikimedia Commons for LibriVox

- Owner decision supersedes the earlier Aesop replacement idea. LibriVox remains a deeply frozen registry compatibility entry so accepted creations can still resolve `librivox/book`, but `selectionEnabled: false` removes it from every spin and from live/hourly health probes. The selector's real 21-entry registry was exercised across 5,000 deterministic seeds with zero LibriVox selections; the public registry reports the disabled state. Existing LibriVox artifacts remain immutable and retain the shared `Promise.allSettled` per-source failure guidance.
- The exclusion is evidence-based, not a transient health demotion: RSS resolution measured **12.6 seconds**, media TTFB measured **13–18 seconds**, the `librivox.org` → `archive.org` two-stage path adds two independent failure surfaces, and book `id=47` exposes a roughly **20 MB** enclosure that was observed failing the fixed 8 MiB page-load policy as `media_bytes_cap`. The runtime adapter and archive host policy remain only for frozen-creation compatibility.
- Wikimedia Commons Audio is the selectable replacement. The fixed keyless query is `field recording filetype:audio filesize:>100`, uses four results and only `imageinfo` URL/size/MIME/`LicenseShortName`. One bounded implementation capture returned four candidates; the adapter rejected 585,836,712, 33,264,080, and 10,471,718-byte files before signing and selected the 2,186,836-byte Commons-hosted recording. Only exact `upload.wikimedia.org` media URLs pass the same mint/serve validator. Live mode range-probes the candidate before minting; adapted output exposes bounded title, byte size, MIME, license, and a same-origin signed `mediaUrl`, never the upstream URL.
- The registry now contains **21 compatibility entries and 20 selectable entries**. The Commons raw fixture, production-adapted golden, generated response example/output schema/semantic paths, 🔔 display symbol, policy URLs, five-minute cache, health integration, and fixture parity checks all derive from the same fixed operation. `registry:verify` reports the two counts separately; disabled LibriVox produces an explicit `owner_selection_disabled` health row without an upstream call.
- Independent read-only review reported no Critical findings and two Important contract holes. A missing D1 health row could fail open, so selection now treats every absent/non-healthy registry row as unhealthy until publication. A Commons candidate with missing `pageid`, title, or `LicenseShortName` could violate its generated non-null response schema, so incomplete candidates are rejected before signing. Focused regressions and the full machine gate cover both fixes.
- Pre-deploy evidence: `acceptance:machine` passed **112 unit**, **17 integration**, and **1 containment/e2e** test, build, 21-entry registry verification, security/secrets/document scans, and the 390 px Chromium suite. The Commons integration route served signed `audio/ogg` with wildcard read CORS from exact `upload.wikimedia.org`. Chromium played the signed fixture inside the sandboxed `/run` iframe—not by top-level navigation—with `currentTime=0.006802`, `readyState=4`, visible unobstructed controls, and the existing 742.7 px mobile frame.
- One bounded live registry pass checked the 20 selectable operations exactly once and did not call LibriVox. Commons passed in **1,753 ms** with matching adapted shape. The report remained honest for unrelated providers: TVMaze stayed disabled for shape drift and Open Food Facts remained unavailable; neither was retried or patched.
- Meter continuity: this owner-directed audio launch repair proceeds under the required deployment/acceptance reserve. No fresh meter reading is inferred; the last owner-transcribed values remain weekly plan **0% remaining** and grant balance **2,111/2,500**.

## 2026-07-19 — Owner platform research: audio inside ChatGPT Apps

- Owner research on Apps SDK audio behavior (2026-07-19): user-gesture `<audio>` playback is an officially envisioned widget use case — OpenAI's UI guidance lists self-contained audio-player widgets, and the Spotify app plays playlists directly in chat — while autoplay falls under the browser Permissions-Policy whose default allowlist is `'self'`. Microphone capture and WebRTC/WebSocket audio streaming inside widgets remain publicly unanswered (a developer-community question dated 2026-07-03 had no official reply at research time), and ChatGPT Voice Mode does not invoke app tools at all.
- Mapping to this product: generated artifacts already restrict playback to user-initiated native controls (`preload="none"`, no autoplay anywhere), which is exactly the case the platform explicitly supports. The nested widget → `/c` → `/run` chain is declared via `frameDomains` and proven to play in a normal browser (signed stream inside the sandboxed `/run` frame, seek bar advancing). The sole remaining unknown is whether ChatGPT's widget iframe narrows descendant permissions policy; that can only be resolved by direct in-ChatGPT observation and is not claimed here. Voice Mode is excluded from demo and judge instructions because tool calls do not fire from it.
- Preventive contract direction recorded: artifact guidance and static validation must prohibit `autoplay` attributes and load-time `play()` calls — an autoplay attempt would fail silently inside the sandbox and read as a broken specimen rather than a policy block.

## 2026-07-19 — Keeper refresh and generation-side playback/stall contract

- The keeper spec is no longer the preserved dark pre-refresh page. It now uses the same machine-issued paper chrome as `/c`: strict external `/creation.css`, record kicker and stamp, structured receipt fields, numbered causal chain, API-role table, style-cartridge receipt, and persistent specimen/download/showcase navigation. Its CSP is tightened to `style-src 'self'`; no inline style or script is introduced. A production read of existing creation `creation_e5520fd3e8499f25109a0dbd4dea1aa4` confirmed the new `rw-chrome rw-record rw-keeper` page, external stylesheet, and strict CSP.
- The shared artifact policy now rejects only an `autoplay` attribute on an `<audio>` or `<video>` start tag; similarly named data/ARIA text remains valid. Every artifact-facing contract surface says playback begins only from an explicit user action and forbids load-time or data-arrival `play()`. It also requires an honest nearby resolving state such as `TUNING THE CARRIER…`, delays `src` assignment and enabled-looking controls until a signed `mediaUrl` exists, forbids fake buffering, and applies the existing per-source failure line to `ok:false` or missing media. Broker, signed-media serving, containment, choreography limits, and frozen artifacts are unchanged.
- Accepted-artifact and repair guidance now targets **10–16 KiB of compact, dense code** while preserving the 10,000–40,000-byte validator band. If `submit_artifact` or `submit_repair` is interrupted or returns no tool result, the model is told to repeat the identical call once with the same `requestId`; server idempotency prevents duplicate acceptance. Widget auto-nudges and copyable manual fallback prompts carry the same phase-specific instruction. This is a mitigation for the recorded platform stall, not a claimed reduction in stall rate; the owner acceptance protocol remains the source of that observation.
- Focused verification only, per the owner's explicit stop: format, lint, typecheck, build, document, security, and secrets checks passed; **114 unit**, **17 integration**, and **1 local containment/e2e** test passed. Local Chromium confirmed the keeper paper CSS (`3px` border under strict CSP), the 742.7 px mobile and 792 px desktop frames, semantic fixture rendering, and user-gesture signed audio inside `/run` (`currentTime=0.011225`, `readyState=4`, visible/unobstructed). The long deployed E2E and live API probes were not resumed.
- Worker version `1e9fd219-e2d0-4c2d-96ba-0b38708f27ce` is deployed at `https://randomware.randomware.workers.dev`; the minimal post-deploy health and keeper reads passed. Because the widget template changed, refresh the ChatGPT connector (sometimes twice) or recreate it before judging the new nudge/fallback copy.
- Meter checkpoint is owner-transcribed, not inferred: weekly plan **0% remaining**, grant balance **590/2,500** (a **1,521-credit** decrease from the prior 2,111 snapshot). No optional verification or provider retry was performed after that direction.

## 2026-07-20 — Owner verification: in-ChatGPT audio resolved, deployment traceability, R1 pre-submission review

- In-ChatGPT audio is resolved by observation, not by a code change. On the current deployment (Worker version `1e9fd219-e2d0-4c2d-96ba-0b38708f27ce`), the owner ran a creation inside the ChatGPT widget iframe and observed audible playback from a user-gesture start. The earlier "no audible playback inside ChatGPT" observation is most plausibly explained by a stale connector template or by testing before the `/media` cross-origin-resource-policy correction propagated; the exact cause was not instrumented. The planned diagnosis-visibility round was cancelled before any implementation and consumed no credits. This closes the sole remaining unknown recorded in the 2026-07-19 platform-research entry; Voice Mode remains excluded from demo and judge instructions because tool calls do not fire from it.
- Deployment traceability, recorded retroactively: the Commons audio roster replacement (commits `931a5cc` and review-fix `552ac4a`) reached production, but its round entry recorded only pre-deploy evidence and omitted its own deploy line. The live surface confirms the shipped state today: `/healthz` reports `registry: 21` and the Commons audio entry is selectable. The most recent recorded deployment is Worker version `1e9fd219-e2d0-4c2d-96ba-0b38708f27ce`; the repository head at the time of this entry is `18dcf71`. The submitted version will be identified by a git tag during the final release ceremony.
- An independent pre-submission review (R1: a fresh Claude subagent with no shared conversation context, strictly read-only) audited the repository at `18dcf71` and the deployed surface against the PRD §16 submission contract, the ACCEPTANCE checklists, and the honesty of public claims. Verdict: **READY** to proceed to the owner acceptance protocol, with zero Critical findings. Every finding was reproduced by the orchestrator before acceptance; none were vacuous. The four Important findings are addressed in this commit set: the README now leads with the zero-setup deployed showcase and no longer labels the local companion zero-setup, README media now includes two live-surface screenshots under `docs/media/`, the registry count is stated precisely (21 compatibility entries, 20 selectable), and this entry supplies the missing deploy traceability. The submission-tag finding is deferred to the release ceremony by design. Of the Minor findings, the project description now states how APIs are selected; the suggestion to mention the Luna→Sol escalation in `DEMO_SCRIPT.md` was rejected to protect the 2:50 budget.
- Meter continuity: no Codex or model work was performed for this round (owner-side documentation only); the last owner-transcribed values remain weekly plan **0% remaining** and grant balance **590/2,500**.

## 2026-07-20 — Owner acceptance protocol (§3.2): 10-spin run complete

- Conditions: every spin ran in real ChatGPT on GPT-5.6 Sol at high reasoning effort (owner-standardized after observing lower-effort settings composing artifacts unreliably). The deployment was frozen throughout: Worker version `1e9fd219-e2d0-4c2d-96ba-0b38708f27ce`, source `18dcf71`; commits `207b89f`/`053a433` and this entry are documentation-only and do not touch the Worker. The run spanned 00:51–04:42 UTC in one ceremony with a ~2 h owner break between rows 9 and 10; per-row timing is server-derived from each run's own `spin_received` event, so the break does not distort any row.
- Protocol amendment, owner-ruled and recorded before row 3: §3.2 fixes "10 fresh 2-API spins", but the deployed selector draws a third API at a seed-random ~15% rate the owner cannot control. The run therefore counts every spin toward the 10 rows and records arity per row. No pass threshold changed; this is a sample-definition correction toward the real product distribution, not a loosening.
- The 10 rows (all server-verified from run summaries and broker logs):

| # | app | APIs | style | concept s | spin→preview s | boot | repairs |
|---|-----|------|-------|-----------|----------------|------|---------|
| 1 | Gallery Supply Raid (`run_e6cce8af…`, `creation_5194f639…`) | artic × frankfurter × themealdb | video-game-hud | 69 | 335 | pass | 0 |
| 2 | Canine Customs Ledger (`run_492a628e…`, `creation_5f5d8426…`) | deck-of-cards × dog-ceo × frankfurter | paper-certificate | 49 | 253 | pass | 0 |
| 3 | Seismic Time Mixer (`run_01af3398…`, `creation_29a9d9e0…`) | wiki-onthisday × usgs-quakes | flash-app | 126 | 329 | pass | 0 |
| 4 | Quiet Walk Registry (`run_4c20afbf…`, `creation_3a59a11d…`) | datamuse × dog-ceo × nager-date | paper-certificate | 33 | 390 | pass | 0 |
| 5 | Snack Signal Quest (`run_b0d53843…`, `creation_1b079cd7…`) | open-food-facts × wikimedia-commons-audio | retro-90s-pixel | 312 | 576 | pass | 0 |
| 6 | Gallery Supper Signal (`run_b686bf3e…`, `creation_e2813e5e…`) | met-museum × themealdb | teletext | 94 | 282 | pass* | 0 |
| 7 | Faultline Muse Summon (`run_c23ea6d8…`, `creation_a8289bfc…`) | usgs-quakes × met-museum | gacha-app | 181 | 320 | pass | 0 |
| 8 | Temporal Weather Warrant (`run_d5b4fdc6…`, `creation_27349551…`) | open-meteo × wiki-onthisday | paper-certificate | 44 | 264 | pass | 0 |
| 9 | Weatherbound Familiar (`run_37c3fff5…`, `creation_3e6e5824…`) | poetrydb × dog-ceo × open-meteo | gacha-app | 267 | 566 | pass* | 0 |
| 10 | Portal Cartridge (`run_954f8c0a…`, `creation_8a6da253…`) | rickandmorty × nasa-images | retro-90s-pixel | 46 | 251 | pass | 0 |

- Pass calculations: booted interactive **10/10** (threshold ≥7/10, PASS). Broker completion for every selected API among booted rows **10/10** (threshold `ceil(0.8×10)=8`, PASS); every row's selected APIs appear as `ok` broker rows generated by real owner interaction. Distinct combinations **10/10 all distinct** (threshold ≥5, PASS), spanning visual, audio, geo, text, and number/state plus dates, food, and weather. Median spin→concept **81.5 s** and median spin→first-preview **324.7 s (~5.4 min)** both MISS the ≤10 s / ≤90 s owner-demo targets and are recorded as misses without tuning: the contract triggers tuning only when the core flow is broken or the PRD metric sample fails, and neither happened (10/10 boot, zero repairs). The miss magnitude is model-composition time on the platform, consistent with the previously recorded ~3-minute healthy-build standard. Repairs **0/10**; stalls inside the denominators **0**; auto-nudge fired **0** times.
- Two rows carry honest blemishes. Row 6 first paint showed a broken image that recovered on data arrival — the artifact contract mandates a pre-arrival honest state for audio (`src` must wait for the signed URL) but has no image equivalent; queued as a guidance-parity fix. Row 9's first interaction press inside the ChatGPT embed errored with zero broker trace; the owner's second press succeeded, and an orchestrator web reproduction of the same specimen rendered live values from all three sources (34.9 °C / code 0, a Browning poem, dog portrait served). The broker does not log gate-rejected calls, so a zero-row failure is not diagnosable server-side — recorded as a post-submission observability item.
- Voided trial (excluded from every denominator and from the stall numerator): one spin (`run_aadf197f…`, randomuser × wikimedia-commons-audio) was started and the owner immediately navigated away from the conversation. The widget unmounted, no auto-nudge could fire, and the run died honestly at `choreography_timeout` detail `concept` after 8 m 41 s with the widget's death notice and inspectable record working end-to-end. Because operator absence and the known platform stall cannot be separated in this sample, it is voided rather than counted as either a product failure or a stall observation.
- Anomaly, root-caused: ChatGPT now interposes an intermediate "thinking / respond now" UI on app invocation and auto-drives the tool chain on a bare mention. On one spin the model auto-spun (`run_4c20afbf…`, 01:31:54) and the owner's widget Spin press created a sibling run 11 s later (`run_26e91a3b…`, orphaned in `spinned`). `createRun` requestId idempotency is present in code and was not the gap — these were two distinct calls. The operating procedure was changed for the remaining rows (invoke with an explicit "open only, wait for the widget button" instruction; never press respond-now mid-flight) and no further duplicates occurred. A tool-description defense is queued.
- Mutation requirement: the owner mutated row 9's combination twice. M1 (escape-maze poster: poem as route cipher, dog portrait as hidden exit key, weather as maze-rotation rules) executed `mutate_creation` successfully; the model then presented the original frozen creation's URL as "the mutated work", which is misleading copy worth noting — mutation is a concept-level record by design and rebuilds nothing. M2 (missing-dog investigation board: weather as search conditions, poem as fragmented witness testimony, dog portrait as the missing subject) was fully composed by the model, but the `mutate_creation` call was blocked twice in a row — the model reported no tool access on both attempts, a textbook instance of the documented intermittent safety-layer swallowing of parameterized calls; the exact payload (creationId + English premise) is preserved in the conversation. The owner judged both mutations meaningfully different from the original (summon-gacha → escape maze → investigation board, with all three API roles reassigned each time): PASS. Separately noted: `mutate_creation` returns "Mutation recorded" but persists nothing server-side — queued post-submission as persist-or-reword.
- Eccentricity check: §3.2 asks for a separate fixed 10-concept sample; with owner approval the run's own 10 accepted concepts were judged instead, and the deviation is recorded here. Zero to one of the ten (Quiet Walk Registry as the generous borderline) could pass as a sincere startup pitch — far below the fewer-than-5 line: PASS.
- Platform evidence gathered during the ceremony, for the judge-facing caveat: parameterized tool calls were swallowed twice consecutively (M2), the new auto-drive/respond-now UI produced one duplicate-spin collision (A1), and signed audio played inside the ChatGPT widget from a user gesture on row 5 — further confirmation on a fresh specimen.
- Meter: the owner transcribed grant balance **435/2,500** mid-run (155 below the prior 590 checkpoint; the drop is not attributed to any logged work in this repository, and the owner observed the meter not moving in real time during spins). No Codex work was performed in this ceremony. The pre-submission repair round is scoped to guidance/copy-only with focused verification accordingly.

## 2026-07-20 — Focused acceptance repair: recommendation, spin guard, and image/fixed-operation guidance

- The owner supplied the acceptance evidence driving this small reserve round: boot **10/10**, one auto-spin collision under the new ChatGPT thinking/respond-now UI, one first-paint broken image that recovered on arrival (row 6), and one fixed-operation gacha repeating its output (row 7). Only guidance, tool text, and widget template state/copy were changed. Broker, containment, choreography, storage, registry behavior, and frozen artifact HTML remain untouched.
- The widget guidance now visibly says **BEST WITH GPT-5.6 SOL (HIGH REASONING)**. The same recommendation is included in MCP initialize instructions and every tool description, so the model can pass it to the user. `open_randomware` and `spin_apis` descriptions also explicitly say not to call `spin_apis` unless the user explicitly asks for a spin or the widget button initiated it, and not to self-spin after an interruption.
- The shared artifact prompt/manifest now teaches image parity with audio: do not set `img.src` until a signed URL arrives in `result.data`; show an honest nearby image-loading state; and never render a broken image icon as the initial state. No static image rule was added, avoiding false rejection of valid artifacts. The same surfaces state that registry operations use fixed parameters; unless a source is inherently time-varying, do not promise changes every call, and fixed-operation gacha/summon behavior must not claim fresh output each time.
- The widget now has an in-flight and active-run re-entry guard. The Spin button is disabled while the widget-owned run is progressing, a second DOM click is ignored, and it becomes available again only for a terminal completed/failed run. Initial call failure still re-enables it. The guard is template-level only and does not alter server idempotency or run state.
- Focused verification passed: format, lint, typecheck, build, security scan, secrets scan, and document check; **116 unit tests** passed. Local Chromium at 390 px confirmed the recommendation line, active-run double-click guard, keeper paper surface, responsive frame, semantic rendering, and user-gesture signed audio inside `/run` (`currentTime=0.009584`, `readyState=4`, controls visible/unobstructed). No integration suite, deployed E2E, or live-provider probe was run in this reserve round.
- Worker version `ec9d09c5-140a-4df4-9200-4d73a4de761d` is deployed at `https://randomware.randomware.workers.dev`. The minimal post-deploy read returned `healthz {ok:true, registry:21}` and `tools/list` returned 8 tools; all tool descriptions contained the recommendation and the two spin surfaces contained the auto-spin guard.
- Owner-side follow-up: after deployment, refresh the ChatGPT connector (sometimes twice) or recreate it before the manual-check ceremony, then perform one smoke spin first. The owner-supplied 10-spin evidence remains the acceptance baseline; this round makes no claim that platform latency or stall rates improved.
- Meter is owner-transcribed, not inferred: weekly plan **0% remaining**, grant balance **435/2,500**. The 155-credit delta from the prior 590 checkpoint is not attributed to repository work.
- Post-round owner transcription (2026-07-20, after deployment): grant balance **409/2,500**, weekly plan **0% remaining**. The 26-credit delta spans this guidance round and is consistent with its focused-only scope. Free margin above the 300-credit reserve is now **109**; any further model work before submission stays within that margin under the same reserve policy.

## 2026-07-20 — Focused smoke-spin correction: terminal guards and creation URL output

- Owner smoke-spin evidence on Worker `ec9d09c5-140a-4df4-9200-4d73a4de761d` found two semantic gaps. The Spin button looked permanently disabled after its first use, and accepted artifact/repair tool text did not explicitly make the finished specimen URL the model's user-facing handoff. This reserve round changed only the widget template, MCP/tool text, and the focused regression coverage; broker, containment, choreography, storage, registry behavior, and stored artifact HTML were not touched.
- Widget lifecycle semantics are now explicit and terminal-safe. Spin is disabled only while the widget-owned run is in progress; `completed` or `failed` clears the active-run guard and permits a new spin. The Build button is idempotently disabled from its first click through terminal completion/failure, while the existing initial follow-up failure path clears the guard immediately so the user can retry. The template's terminal and fallback paths all resynchronize both guards.
- Accepted `submit_artifact` and `submit_repair` results now return concise text that presents `creationUrl` as a link and says not to list run IDs or internal details. Their tool descriptions carry the same instruction, so the model has the handoff rule before it submits.
- Focused verification only: `format:check`, lint, typecheck, build, security scan, secrets scan, and document check passed; **118 unit tests** passed. Local fixture-mode Chromium at 390 px completed the full widget lifecycle (Spin click → in-progress disabled → terminal re-enable → second spin → Build disabled → terminal re-enable), plus the existing widget/reel/heartbeat/fallback/creation checks. This run used `RANDOMWARE_BROWSER_REQUIRE_AUDIO=0`; no deployed E2E or live-provider probe was run.
- Deployment: Worker version `2f65f5b7-c4d4-4385-bd00-0b8b1690e439` is live at `https://randomware.randomware.workers.dev`. The minimal post-deploy reads returned `healthz {ok:true, registry:21}` and an 8-tool manifest; the deployed `open_randomware`/`spin_apis` descriptions retain the explicit auto-spin guard and the accepted-artifact descriptions include the creation URL handoff.
- Owner action: refresh the ChatGPT connector (sometimes twice) or recreate it, then perform one smoke spin. Meter is owner-transcribed, not inferred: weekly plan **0% remaining**, grant balance **409/2,500**; no additional meter delta is attributed to this round.

## 2026-07-20 — Widget re-instantiation guard correction

- Owner reproduction on Worker `2f65f5b7-c4d4-4385-bd00-0b8b1690e439` disproved the prior single-context claim: after a real ChatGPT completion, Spin remained unavailable even though the fixture lifecycle test had reached a terminal phase. Static tracing confirmed that `run` was reconstructed when synchronous `widgetState.runId` existed, but a missing phase was coerced to `spinned`; the first status read was delayed three seconds, and a failed read left that unverified phase permanently authoritative in both `syncSpinGuard()` and `spinNow()`. ChatGPT can create a fresh widget JavaScript context per result or redisplay, so the local-variable continuity assumption was the root defect.
- Restored state is no longer trusted as live server state. A widget with a persisted run immediately fetches its absolute `statusUrl`, disables Spin only during a bounded 1.5-second restoration window or for a server-confirmed active phase, and then uses the confirmed phase. If restoration cannot complete, the guard fails open and background status polling continues; a rare duplicate spin remains bounded by request idempotency and the existing orphan exclusion, while a permanently unusable control is avoided. `spinNow()` and button rendering now share the same `hasTrustedActiveRun()` predicate.
- The previous behavior-changing string `.replace()` patches were removed. Build and Spin guards, terminal/fallback resets, restoration trust, and fail-open handling now live directly in the shipped widget script. The remaining template replacement only injects the static model-recommendation copy.
- TDD evidence: the new fresh-context Chromium test first failed with `widget_restored_run_not_rehydrated_immediately`. After the correction, local fixture-mode Chromium at 390 px passed all four mount paths: no persisted run, server-confirmed active restored run (Spin blocked), server-confirmed terminal restored run (Spin enabled and callable), and failed restoration (Spin enabled after the bound). The existing same-context Spin/Build lifecycle also remained green.
- Focused verification passed: format, lint, typecheck, build, security scan, secrets scan, document check, and **119 unit tests**. Chromium ran with `RANDOMWARE_BROWSER_REQUIRE_AUDIO=0`; no deployed E2E, live-provider probe, integration suite, or audio check was run.
- Deployment: Worker version `1da974dd-9a95-40a2-a1e2-484771773493` is live at `https://randomware.randomware.workers.dev`. The first immediate resource read reached the prior 95,858-byte widget during propagation; one bounded retry returned the new 97,309-byte template with `rehydrateRestoredRun`, `hasTrustedActiveRun`, and the 1.5-second fail-open bound. `healthz` remained `{ok:true, registry:21}`.
- Owner action: refresh the ChatGPT connector (sometimes twice) or recreate it, then run one smoke spin. Meter is owner-transcribed, not inferred: weekly plan **0% remaining**, grant balance **392/2,500**; the exact decrease from the prior owner-transcribed 409 balance is **17**, leaving **92** above the 300-credit reserve.
